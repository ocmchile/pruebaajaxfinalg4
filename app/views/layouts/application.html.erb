<!DOCTYPE html>
<html>
<head>
  <title>Pruebaajaxfinal</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>

<a href="<%= companies_path %>">Companies</a> | <a href="<%= complains_path %>">Complains</a> | 
<% if user_signed_in? %>
Hola <%= current_user.name %>: <%= link_to "Salir", destroy_user_session_path, method: :delete %>
<% else %>
<%= link_to "Log in", new_user_session_path %>
<%= link_to "Regístrate", new_user_registration_path %>
<% end %>

<p class="notice"><%= notice %></p>
<p class="alert"><%= alert %></p>

<%= yield %>

</body>
</html>
